import { LegalDocument } from "./LegalDocument";

/*
 * TermsPage
 * ---------
 * The terms of service. See PrivacyPage — same renderer, different document.
 */

const TermsPage = () => <LegalDocument kind="terms" />;

export default TermsPage;
